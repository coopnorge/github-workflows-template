on:
  workflow_call:
    inputs: {}
    secrets: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  example-job:
    runs-on: ubuntu-24.04
    steps:
      - run: echo hello world
